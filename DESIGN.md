# Design

## 1. Overview

这是一个轻量路线调度工具。视觉方向是“白天使用的城市调度台”：明亮、清晰、信息密度高，地图居中，左右两侧像工作栏而不是营销页面。

## 2. Colors

使用 OKLCH 色彩。中性底色偏冷，保证地图和路线层可读；强调色只用于路线、司机、集合点、确认状态和主操作。

- Canvas: `oklch(95.5% 0.012 238)`
- Surface: `oklch(98.7% 0.007 238)`
- Ink: `oklch(22% 0.026 245)`
- Muted: `oklch(49% 0.035 245)`
- Route blue: `oklch(53% 0.18 255)`
- Driver orange: `oklch(66% 0.17 52)`
- Meeting plum: `oklch(49% 0.14 318)`
- Success green: `oklch(57% 0.14 158)`
- Warning amber: `oklch(73% 0.15 82)`

## 3. Typography

使用系统无衬线字体栈。产品界面不使用展示字体。标题通过字号和字重拉开，标签保持紧凑，长说明控制在 65 到 75 字符以内。

## 4. Elevation

面板用 1px 分割线和浅阴影，避免厚重卡片。地图上的浮层使用实体浅底和清楚边框，不使用模糊玻璃。

## 5. Components

- Top bar: 产品名、简短状态、城市输入、当前人数/司机/集合点统计。
- Input rail: 终点、人员、集合点三段清单，底部固定主操作。
- Map stage: 地图为主视觉，叠加轻量状态条和图例。
- Result rail: 未规划时显示清晰空状态，规划后显示指标、接人顺序、集合路线和备选方案。
- Form controls: 统一高度、边框、焦点环和禁用态；下拉框默认留空。

## 6. Do's & Don'ts

Do:

- 保留标准表单交互。
- 用编号、标签和文字解释路线。
- 让地图和结果面板共同承担决策。
- 为长姓名、长地址、多人、多司机预留换行和滚动。

Don't:

- 不使用紫色大渐变或玻璃卡片。
- 不把所有内容放进同样大小的卡片网格。
- 不用装饰图形替代真实地图。
- 不让移动端横向滚动。
